# Photos / large binaries
*.jpeg
*.jpg
*.png

# ESP-IDF build output (regenerated by `idf.py build`)
build/

# ESP-IDF generated / machine-local config
sdkconfig.old
managed_components/
dependencies.lock

# Editor / OS cruft
.vscode/
.DS_Store

# Note: sdkconfig IS committed on purpose (no sdkconfig.defaults in these projects,
# so sdkconfig is the only record of target + config). Delete it here to ignore instead.
